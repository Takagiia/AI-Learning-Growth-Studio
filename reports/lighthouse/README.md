# Lighthouse 报告

将 Lighthouse 导出的 JSON 文件放入此目录，即可由 AI 助手读取分析。

## 如何导出

1. Chrome DevTools → Lighthouse 标签页
2. 点击 "Analyze page load"
3. 报告生成后，点击右上角 "Export as JSON"
4. 将下载的 `.json` 文件放入此目录

## 文件命名建议

`<页面名>-<日期>.json`，例如：`dashboard-2026-05-31.json`
